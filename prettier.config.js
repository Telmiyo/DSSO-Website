const { ModalFocusScope } = require("@chakra-ui/react")

const options = {
    arrowParens: "avoid",
    singleQuote: false,
    bracketSpacing: true,
    endOfLine: "lf",
    semi: true,
    tabWidth: 2,
    trailingComma: "none"
}

module.exports =  options